# 模型服务能力记录

项目使用本地配置指定的端点；当前配置采用 MiniMax 的 Anthropic 兼容消息协议。协议格式兼容不代表支持某个 vendor 的全部扩展。

这份文档只记录实际运行过的能力探测，不预填未来功能可能需要的矩阵。某项当前实现确实依赖缓存、流式输出、推理块往返、`usage` 语义或上下文上限时，先写最小探测并征得运行真实 API 的同意，再新增记录。

每条记录必须包含：

- 为什么当前实现依赖这项能力；
- 可重复运行的命令或脚本；
- 端点、模型、SDK 版本和日期；
- 原始错误码、响应字段或 `usage` 证据；
- 不支持或结论不明确时的降级方案。

| 日期 | 能力问题 | 环境 | 证据 | 结论与降级方案 |
|---|---|---|---|---|

目前还没有运行过模型服务能力探测。
